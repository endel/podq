{
  "scripts": {
    "postinstall": "npm --prefix ./frontend/player install && npm --prefix ./frontend install && gulp --cwd ./frontend build"
  },
  "engines": {
    "node": "4.1.1"
  },
  "devDependencies": {
    "gulp": "^3.8.11"
  }
}

